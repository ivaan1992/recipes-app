<% if user_signed_in? %>
  <div class="navbar">
    <div class="welcome">
      Welcome <%= user.email %>!
    </div>
    <%= button_to 'Sign Out', destroy_user_session_path, method: :delete, class: "out_btn" %>
  </div>
<% end %>